import React from "react";
import { createRoot } from "react-dom/client";
import PairMixtape from "./PairMixtape.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PairMixtape />
  </React.StrictMode>
);
